


let Numero = Number(prompt("Elegí un Número"))
if (!Number.isNaN(Numero) & Numero) {
    alert("Bien")
} else {
    alert("Dale pelotudo")
    alert("Bueno, vamos de nuevo")
    let Letra =  prompt("Is this the real life?")
    if (Letra & (Letra == "Is this just fantasy?" || "is this just fantasy?")) {
        alert("Ta, te perdono")
    } else {
        alert("Cancelado")
        self.close()
    }
}


